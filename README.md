# FE Horse Racing

Scaffolded Vite + React project for Horse Racing dashboard.

Quick start:

```bash
npm install
npm run dev
```

Notes:
- Backend API expected at `http://localhost:4000/api` (see `src/constants/api.js`).

